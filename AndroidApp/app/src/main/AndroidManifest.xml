<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.adminturnos">
    <!-- PERMISSIONS -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".Activities.Job.edit.ServiceConfigActivity"></activity>
        <activity android:name=".Activities.Job.ViewAppointmentActivity" />
        <activity
            android:name=".Activities.Job.edit.EditJobActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".Activities.JobRequest.NewJobRequestActivity" />
        <activity android:name=".Activities.JobRequest.JobRequestActivity" />
        <activity
            android:name=".Activities.NewPlace.NewPlaceActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".Activities.Place.ViewPlaceActivity" />
        <activity android:name=".Activities.Job.ViewJobActivity" />
        <activity
            android:name=".Activities.SignIn.ActivitySignIn"
            android:theme="@style/AppThemeNoActionBar" />
        <activity
            android:name=".Activities.MainScreen.MainActivity"
            android:theme="@style/AppThemeNoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>